{
  "dependencies": {},
  "name": "live-board",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "docker-compose -f dev-compose.yml up",
    "prod": "docker-compose -f prod-compose.yml up",
    "start:service": "docker-compose -f dev-compose.yml up mongodb redis",
    "lint": "cd server && yarn lint && cd ../client && yarn lint",
    "install-package": "cd server && yarn && cd ../client && yarn"
  }
}
